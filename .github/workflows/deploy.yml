script: |
  cd ~
  if [ ! -d "ai-resume-agent" ]; then
    git clone https://github.com/raheemmohd/ai-resume-agent.git
  fi
  cd ai-resume-agent
  git pull origin main
  docker-compose down
  docker-compose up -d --build
